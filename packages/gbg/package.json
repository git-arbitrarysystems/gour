{
  "name": "gbg",
  "version": "1.0.0",
  "description": "Generic boardgame",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch --verbose",
    "dev": "vite",
    "build": "tsc && vite build",
    "build:watch":"tsc --lib es2020,dom,dom.iterable --target es2020 --moduleResolution bundler --outDir test/lib ./src/*.ts --watch",
    "runner": "nodemon -e ts,js --exec \"tsx ./runner.ts\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "~29.7.0",
    "typescript": "~5.3.2",
    "ts-jest": "~29.1.1",
    "@types/jest": "~29.5.10",
    "tsx": "~4.6.2"
   },
  "dependencies": {
    "vite": "~5.0.7"
  }
}
